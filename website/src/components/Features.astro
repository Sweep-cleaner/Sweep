---
const features = [
  {
    icon: '🦀',
    badge: 'RUST 1.88+',
    title: 'Zero-Dependency Binary',
    description: 'Runs as a single 6.0 MB compiled binary. No Python 3 runtime, no GTK libraries, no Chromium or Electron bloat. Starts in milliseconds.',
    colSpan: 'md:col-span-2',
    gradient: 'from-rust/10 via-rust/5 to-transparent'
  },
  {
    icon: '⚡',
    badge: 'PARALLEL RAYON',
    title: 'Blazing Multi-Core Speed',
    description: 'Scans 12,611 files in just 4.85s on a modest Ryzen 7 laptop. Directory walks saturate NVMe SSD bandwidth across all CPU threads.',
    colSpan: 'md:col-span-1',
    gradient: 'from-cyan/10 via-cyan/5 to-transparent'
  },
  {
    icon: '🛡️',
    badge: 'FAIL-CLOSED SAFETY',
    title: 'Guards That Cannot Be Bypassed',
    description: 'An immutable protected-root set plus your personal keep list are checked before every unlink — and `--force` cannot switch them off. Traversal never follows symlinks or Windows reparse points, never crosses a mount boundary, and refuses relative paths instead of resolving them against the working directory. Untrusted community definitions lose their `process` and `winreg` powers unless you explicitly opt in, and the operation log is opened with `O_NOFOLLOW`.',
    colSpan: 'md:col-span-2',
    gradient: 'from-amber-500/10 via-amber-500/5 to-transparent'
  },
  {
    icon: '🎨',
    badge: 'QT6 / QML',
    title: 'Native Qt6/QML Desktop Shell',
    description: 'The `sweep-qml` shell drives the same `--json` CLI across 13 screens and 11 built-in themes, with an animated dashboard, system tray, notifications and Windows 11 Mica. Renders on the software scene graph by default, with hardware acceleration one flag away.',
    colSpan: 'md:col-span-1',
    gradient: 'from-cyan-glow/10 via-cyan-glow/5 to-transparent'
  },
  {
    icon: '🧠',
    badge: 'SYSTEM INTELLIGENCE',
    title: 'Beyond Disk Cleaning',
    description: 'Native memory optimization per OS, a startup manager with high/medium/low impact analysis, `sysinfo --health` with temperature and SMART reporting, network cache flushing, and a privacy shield that turns telemetry off and wipes recent files and the clipboard — every command with `--dry-run`.',
    colSpan: 'md:col-span-2',
    gradient: 'from-rust/10 via-rust/5 to-transparent'
  },
  {
    icon: '🎯',
    badge: 'SMART HUNTERS',
    title: 'Specialized Cleanup Hunters',
    description: 'Dedicated modules for developer cruft (`devscan`), uninstalled-app leftovers with package-manager ownership verification, content-hash duplicate detection with `--keep`/`--link`, and a residue engine that scores how safe a finding is to delete.',
    colSpan: 'md:col-span-1',
    gradient: 'from-cyan/10 via-cyan/5 to-transparent'
  },
  {
    icon: '🌍',
    badge: 'COMMUNITY & I18N',
    title: '202 Cleaners, 8 Languages',
    description: 'Shipped with 202 built-in TOML cleaner definitions, a BleachBit CleanerML importer, Winapp2.ini community-rule translation, and a Qt translator localized in English, Turkish, Spanish, French, German, Russian, Portuguese and Italian — with full key parity across all eight.',
    colSpan: 'md:col-span-3',
    gradient: 'from-cyan/10 via-cyan/5 to-transparent'
  }
];
---

<section id="features" class="section">
  <div class="text-center mb-16">
    <p class="section-kicker">// WHY SWEEP?</p>
    <h2 class="text-3xl sm:text-4xl md:text-5xl font-bold text-white mb-4">
      Engineered for Speed, Built for Safety.
    </h2>
    <div class="section-divider"></div>
    <p class="text-text-muted text-base sm:text-lg max-w-2xl mx-auto">
      Every line of code is purpose-built to maximize disk recovery while safeguarding your personal data and development environments.
    </p>
  </div>

  <!-- Bento Grid -->
  <div class="grid md:grid-cols-3 gap-6 max-w-6xl mx-auto">
    {features.map((f) => (
      <div class:list={[
        'card group relative overflow-hidden flex flex-col justify-between',
        f.colSpan
      ]}>
        <!-- Subtle gradient backdrop -->
        <div class={`absolute inset-0 bg-gradient-to-br ${f.gradient} opacity-50 group-hover:opacity-100 transition-opacity duration-500 pointer-events-none`}></div>

        <div class="relative z-10">
          <div class="flex items-center justify-between mb-4">
            <span class="text-3xl">{f.icon}</span>
            <span class="text-[10px] font-mono font-bold px-2.5 py-1 rounded-full bg-bg-elevated border border-border/80 text-text-dim group-hover:text-cyan group-hover:border-cyan/50 transition-colors">
              {f.badge}
            </span>
          </div>

          <h3 class="text-xl font-bold text-white mb-3 group-hover:text-cyan transition-colors">
            {f.title}
          </h3>

          <p class="text-sm text-text-muted leading-relaxed">
            {f.description}
          </p>
        </div>

        <div class="mt-6 pt-4 border-t border-border/30 flex items-center justify-between text-xs font-mono text-text-dim group-hover:text-text-primary transition-colors">
          <span>Explore architecture</span>
          <span class="text-cyan group-hover:translate-x-1 transition-transform">→</span>
        </div>
      </div>
    ))}
  </div>
</section>
